disabled_rules:

- identifier_name
- force_cast
- colon
- syntactic_sugar


excluded: # paths to ignore during linting. Takes precedence over `included`.

- Carthage

- Pods

force_cast: warning
identifier_name: warning

